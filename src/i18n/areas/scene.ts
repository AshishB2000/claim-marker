/** Messages for the diagram and the document: Diagram.tsx, Review.tsx, ReportDocument.tsx, MapScene.tsx, the damage marker. */
export const en = {} as const

export const es: Record<keyof typeof en, string> = {}
